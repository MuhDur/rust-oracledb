profile	command
minimal	cargo check -p oracledb --locked --no-default-features
default	cargo check -p oracledb --locked
all-features	cargo check -p oracledb --locked --all-features
integration-matrix-check	cargo hack check -p oracledb --locked --feature-powerset --depth 1 --include-features chrono,uuid,serde_json,rust_decimal,arrow,soda
integration-matrix-lib-test	cargo hack test -p oracledb --locked --lib --feature-powerset --depth 1 --include-features chrono,uuid,serde_json,rust_decimal,arrow,soda
protocol-stable	cargo +stable test -p oracledb-protocol
fuzz-build	cargo fuzz build --target x86_64-unknown-linux-gnu (cwd crates/oracledb-protocol)
