name: Soak

on:
  schedule:
    - cron: "43 4 * * 0"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  soak:
    uses: ./.github/workflows/_quality.yml
    with:
      profile: soak
      budget: soak
