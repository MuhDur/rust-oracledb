# Operator-specific confidential literals (NEVER commit this file).
# Copy to .secret_scan_denylist and add one extended-regex per line.
#
# Example shapes (replace with your operator's real values):
# mycustomer-codename
# adb\.eu-[a-z0-9-]+\.oraclecloud\.com